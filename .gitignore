node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

.dev.vars*
!.dev.vars.example
!.env.example
# 生产/staging 密钥文件（wrangler secret bulk 用），绝不入库
.prod.vars
.staging.vars

# 本地 Wrangler 配置（含真实 D1/KV id、自定义域名）——不入库；
# 模板参考见提交的 wrangler.example.jsonc，首次 `cp wrangler.example.jsonc wrangler.jsonc`
wrangler.jsonc

# Local-only verification screenshots (not distributed with the template)
docs/superpowers/archive/screenshots/

# Playwright e2e artifacts
e2e/screenshots/
test-results/
playwright-report/
.playwright/

.source/

# Claude Code session config (local)
.claude/

# 大型业务媒体文件——由 Cloudflare R2 + CDN 提供。
# downloads/quality 源文件体积可控,随仓库提交(与产品图一致),由 CI 上传 R2;
# videos/images 仍为 git-ignored(体积大/图源待定),由 scripts/upload-site-assets.mjs 统一上传。
public/assets/videos/
public/assets/images/

dev-server-opt.err
